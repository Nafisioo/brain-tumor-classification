- name: Start container with mounted artifact
        run: |
          docker run \
          -d \
          --name brain-tumor-api-test \
          -p 8000:8000 \
          -v ${{ github.workspace }}/artifacts:/app/artifacts \
          --entrypoint uvicorn \                  
          brain-tumor-api:test \
          api.main:app --host 0.0.0.0 --port 8000     